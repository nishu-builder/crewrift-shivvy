[project]
name = "crewrift-shivvy"
version = "0.1.0"
description = "Shivvy: a CrewRift social-deduction player and its dev harness."
requires-python = ">=3.11,<3.13"
dependencies = [
    # >=0.1.20: earlier versions reject the server's new image-upload response
    # (authorization_token instead of credentials), breaking `upload-policy`.
    "coworld[auth]>=0.1.20",
]

[tool.uv]
package = false
